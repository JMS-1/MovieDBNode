/* eslint-disable @typescript-eslint/naming-convention */

export enum routes {
    container = '/container',
    genre = '/genre',
    language = '/language',
    recording = '/recording',
    series = '/series',
}
